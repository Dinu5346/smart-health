<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="lk.nibm.smarthealth"
    android:versionCode="1"
    android:versionName="@string/txt_app_version">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/heart_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/heart_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHealth">
        <activity android:name=".AboutActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".WaterMinderHistoryActivity"
            android:parentActivityName=".WaterMinderActivity" />
        <activity
            android:name=".WaterMinderActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".UpdateMedicalNoteActivity"
            android:parentActivityName=".MedicalNotesActivity" />
        <activity
            android:name=".AddMedicalNoteActivity"
            android:parentActivityName=".MedicalNotesActivity" />
        <activity
            android:name=".MedicalNotesActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".BmiHistoryActivity"
            android:parentActivityName=".BodyMassIndexActivity" />
        <activity
            android:name=".BodyMassIndexActivity"
            android:parentActivityName=".HomeActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".SignUpActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>